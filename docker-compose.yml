version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      # ===========================================
      # Database Mode (for true self-hosting)
      # ===========================================
      # "sqlite" - Uses local SQLite database (no external dependencies)
      # "supabase" - Uses Supabase (default, requires Supabase credentials)
      - DATABASE_MODE=${DATABASE_MODE:-supabase}
      - DATABASE_PATH=/data/intuneget.db

      # Shared secret between the scheduler below and the cron routes. The
      # routes reject every call while it is unset - they are ordinary HTTP
      # endpoints, so this is all that separates a scheduled call from an
      # anonymous one.
      - CRON_SECRET=${CRON_SECRET:-}

      # ===========================================
      # Packager Authentication (for SQLite mode)
      # ===========================================
      # API key for packager authentication when using SQLite mode
      # Generate with: openssl rand -hex 32
      - PACKAGER_API_KEY=${PACKAGER_API_KEY:-}

      # ===========================================
      # Supabase Configuration (optional in SQLite mode)
      # ===========================================
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL:-}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY:-}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY:-}
      - QA_LIVE_PUBLIC_ENABLED=${QA_LIVE_PUBLIC_ENABLED:-false}

      # ===========================================
      # Microsoft Entra ID (Required)
      # ===========================================
      # NEXT_PUBLIC_AZURE_AD_CLIENT_ID is inlined as empty at Docker
      # build time. AZURE_CLIENT_ID is a non-NEXT_PUBLIC alias that
      # the server component reads at runtime to inject the real value.
      - NEXT_PUBLIC_AZURE_AD_CLIENT_ID=${NEXT_PUBLIC_AZURE_AD_CLIENT_ID}
      - AZURE_CLIENT_ID=${NEXT_PUBLIC_AZURE_AD_CLIENT_ID}
      - AZURE_AD_CLIENT_SECRET=${AZURE_AD_CLIENT_SECRET}

      # ===========================================
      # Packager Mode
      # ===========================================
      # "local" - Use local Windows packager (recommended for self-hosting)
      # "github" - Use GitHub Actions (requires GitHub credentials)
      - PACKAGER_MODE=${PACKAGER_MODE:-local}

      # ===========================================
      # GitHub Actions Pipeline (optional, for github mode)
      # ===========================================
      - GITHUB_PAT=${GITHUB_PAT:-}
      - GITHUB_OWNER=${GITHUB_OWNER:-}
      - GITHUB_REPO=${GITHUB_REPO:-}

      # ===========================================
      # Callback Security (for GitHub mode)
      # ===========================================
      - CALLBACK_SECRET=${CALLBACK_SECRET:-}

      # ===========================================
      # Application URL
      # ===========================================
      - NEXT_PUBLIC_URL=${NEXT_PUBLIC_URL:-http://localhost:3000}

      # ===========================================
      # Optional Features
      # ===========================================
      - NEXT_PUBLIC_PLAUSIBLE_DOMAIN=${NEXT_PUBLIC_PLAUSIBLE_DOMAIN:-}
      - BEEHIIV_API_KEY=${BEEHIIV_API_KEY:-}

    volumes:
      # Persist SQLite database
      - intuneget-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Calls the cron routes on an interval.
  #
  # Those routes are plain HTTP endpoints that a hosting platform is expected
  # to call on a schedule; a container deployment has no such platform, so
  # without this nothing ever ran them. Runs from the same image as the web
  # service, so there is no second image to pull or keep in step.
  #
  # Deliberately no proxy variables: it only ever talks to `web` on the compose
  # network, and inheriting an outbound proxy would send those calls through it.
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["node", "scripts/scheduler.mjs"]
    depends_on:
      web:
        condition: service_healthy
    environment:
      - CRON_SECRET=${CRON_SECRET:-}
      # Where the web service answers, and which jobs to call. The default is
      # the one job that works without Supabase; a Supabase-backed deployment
      # can list the others here as "/path:seconds,/path:seconds".
      - SCHEDULER_TARGET=${SCHEDULER_TARGET:-http://web:3000}
      - SCHEDULER_JOBS=${SCHEDULER_JOBS:-}
    restart: unless-stopped

volumes:
  intuneget-data:
    driver: local
